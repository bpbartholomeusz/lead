document.addEventListener('DOMContentLoaded', () => {
  // const newEventButton = document.querySelector('#new-event-button');
  // newEventButton.addEventListener('click', () => {
  //   // Select the dropdown element (assuming it has an ID of 'eventtype')
  //   const eventTypeDropdown = document.querySelector('#id_eventtype');
  //   console.log({ eventTypeDropdown });
  //   // Remove option by value
  //   Array.from(eventTypeDropdown.options).forEach((option) => {
  //     if (option.value === 'user' || option.value === 'category') {
  //       option.remove(); // Removes the option from the dropdown
  //     }
  //   });
  // });
});
